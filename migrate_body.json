{"query": "CREATE OR REPLACE FUNCTION end_session(p_session_id UUID) RETURNS VOID AS $$ BEGIN UPDATE user_sessions SET ended_at = NOW(), last_heartbeat = NOW() WHERE id = p_session_id; END; $$ LANGUAGE plpgsql SECURITY DEFINER;"}
